syntax = "proto3";

package strat_game.common_definitions;

message BuildingDefinition {
  string id = 1;
  string name = 2;
  uint32 hp = 3;
  uint32 time_to_build = 4;
  int32 observe_modifier = 5;
}

message BuildingIndex {
  repeated string buildings = 1;
}
